import { AlertTriangle, ArrowUpRight, CheckCircle2, ShieldAlert } from 'lucide-react'
import { useState } from 'react'
import { z } from 'zod'
import axiosInstance from '../../api/axiosInstance'
const riskSchema = z.object({ destination: z.string().min(2), severityLevel: z.string().min(1), advisoryMessage: z.string().min(2) })
const risks = [{ destination: 'Sri Lanka · Weather', level: 'Low', message: 'Dry season conditions expected across your route.', updated: 'Updated 2h ago' }, { destination: 'Sri Lanka · Health', level: 'Moderate', message: 'Stay hydrated and carry mosquito repellent for the south coast.', updated: 'Updated yesterday' }, { destination: 'Sri Lanka · Local advisories', level: 'Low', message: 'No active travel restrictions for your destinations.', updated: 'Updated Sep 19' }]
export default function RiskPage() { const [items, setItems] = useState(risks); async function refresh() { try { const payload = { tripId: 1, destination: 'Sri Lanka', severityLevel: 'Low', advisoryMessage: 'No active travel restrictions.' }; await axiosInstance.post('/Risk', payload) } catch { /* The cards remain useful while the API is unavailable. */ } setItems(items) } return <><div className="page-intro"><div><span className="eyebrow">Travel intelligence</span><h1>Know before you go.</h1><p>Helpful signals, clearly surfaced. No doom scrolling required.</p></div><button className="button button-dark" onClick={refresh}><ShieldAlert size={16} /> Refresh radar</button></div><div className="risk-hero"><div className="risk-score"><span>Overall outlook</span><strong>Good to go</strong><div className="risk-meter"><span /></div><small>Based on 12 active signals</small></div><div className="risk-hero-note"><CheckCircle2 size={20} /><span><strong>No critical alerts</strong>Your current itinerary is in a comfortable zone.</span></div></div><section className="risk-list">{items.map((item) => <article className="risk-card" key={item.destination}><div className={`severity-badge ${item.level.toLowerCase()}`}><span />{item.level}</div><div className="risk-card-copy"><h2>{item.destination}</h2><p>{item.message}</p><small>{item.updated}</small></div><ArrowUpRight size={18} /></article>)}</section></> }
export { riskSchema }
