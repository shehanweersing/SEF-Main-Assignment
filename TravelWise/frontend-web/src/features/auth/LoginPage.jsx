import { ArrowLeft, ArrowRight, LockKeyhole, Mail } from 'lucide-react'
import { useForm } from 'react-hook-form'
import { Link, useLocation, useNavigate } from 'react-router-dom'
import { useState } from 'react'
import { useAuth } from '../../context/AuthContext'

export default function LoginPage() { const { register, handleSubmit, formState: { errors } } = useForm(); const { login } = useAuth(); const navigate = useNavigate(); const location = useLocation(); const [error, setError] = useState(''); async function onSubmit(values) { try { await login(values); navigate(location.state?.from?.pathname || '/dashboard') } catch { setError('We could not sign you in. Check your details and try again.') } } return <div className="auth-page"><div className="auth-visual"><Link to="/" className="brand brand-light"><span className="brand-mark">TW</span><span>TravelWise</span></Link><div><span className="eyebrow eyebrow-light">Your journey, in one place</span><h1>Less logistics.<br /><em>More living.</em></h1></div><span className="auth-visual-caption">A thoughtful way to get there.</span></div><div className="auth-form-wrap"><Link to="/" className="back-link"><ArrowLeft size={16} /> Back home</Link><div className="auth-form"><span className="eyebrow">Welcome back</span><h2>Sign in to your trip.</h2><p>Pick up where your planning left off.</p><form onSubmit={handleSubmit(onSubmit)}><label>Email address<span className="input-wrap"><Mail size={17} /><input type="email" placeholder="you@example.com" {...register('email', { required: 'Email is required' })} /></span>{errors.email && <small className="field-error">{errors.email.message}</small>}</label><label>Password<span className="input-wrap"><LockKeyhole size={17} /><input type="password" placeholder="Your password" {...register('password', { required: 'Password is required' })} /></span>{errors.password && <small className="field-error">{errors.password.message}</small>}</label>{error && <div className="form-error">{error}</div>}<button className="button button-dark full-button" type="submit">Continue <ArrowRight size={16} /></button></form><small className="auth-note">New to TravelWise? <Link to="/register" className="text-link">Create an account</Link></small></div></div></div> }
