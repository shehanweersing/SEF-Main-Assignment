import { CalendarDays, Clock3, MapPin, Plus } from 'lucide-react'
import { useState } from 'react'
import { useForm } from 'react-hook-form'
import { z } from 'zod'
import { zodResolver } from '@hookform/resolvers/zod'
import axiosInstance from '../../api/axiosInstance'
const activitySchema = z.object({ title: z.string().min(2, 'Add a title'), location: z.string().min(2, 'Add a location'), startTime: z.string().min(1, 'Select a start time'), endTime: z.string().min(1, 'Select an end time') })
const events = [{ day: '12', month: 'SEP', title: 'Arrive in Colombo', location: 'Bandaranaike International Airport', time: '14:30 - 16:00', type: 'Travel' }, { day: '14', month: 'SEP', title: 'Galle Fort walking tour', location: 'Galle Fort', time: '09:00 - 12:00', type: 'Experience' }, { day: '16', month: 'SEP', title: 'Slow afternoon by the sea', location: 'Unawatuna Beach', time: '15:00 - 18:00', type: 'Leisure' }]
export default function ActivityPage() { const [showForm, setShowForm] = useState(false); const [conflict, setConflict] = useState(''); const { register, handleSubmit, reset, formState: { errors } } = useForm({ resolver: zodResolver(activitySchema) }); async function submit(values) { setConflict(''); try { await axiosInstance.post('/Activity', { ...values, tripId: 1 }); reset(); setShowForm(false) } catch (error) { if (error.response?.status === 409) setConflict('This time overlaps with something already on your itinerary. Try a different time.'); else setConflict('We could not save this activity right now.'); } } return <><div className="page-intro"><div><span className="eyebrow">Your itinerary</span><h1>Make time for wonder.</h1><p>A clear view of the days ahead, with space for spontaneity.</p></div><button className="button button-coral" onClick={() => setShowForm(!showForm)}><Plus size={16} /> Schedule activity</button></div>{conflict && <div className="conflict-message"><Clock3 size={18} /><span><strong>Schedule conflict</strong>{conflict}</span></div>}{showForm && <form className="inline-form activity-form" onSubmit={handleSubmit(submit)}><label>Title<input {...register('title')} placeholder="Activity name" />{errors.title && <small className="field-error">{errors.title.message}</small>}</label><label>Location<input {...register('location')} placeholder="Where is it?" />{errors.location && <small className="field-error">{errors.location.message}</small>}</label><label>Start<input type="datetime-local" {...register('startTime')} />{errors.startTime && <small className="field-error">{errors.startTime.message}</small>}</label><label>End<input type="datetime-local" {...register('endTime')} />{errors.endTime && <small className="field-error">{errors.endTime.message}</small>}</label><button className="button button-dark" type="submit">Save</button></form>}<section className="calendar-panel"><div className="calendar-toolbar"><button className="icon-button">‹</button><strong>September 2026</strong><button className="icon-button">›</button><span className="calendar-view"><CalendarDays size={15} /> Agenda view</span></div><div className="agenda">{events.map((event) => <article className="agenda-item" key={event.title}><div className="date-block"><strong>{event.day}</strong><span>{event.month}</span></div><div className="agenda-line" /><div className="agenda-copy"><span className="eyebrow">{event.type}</span><h2>{event.title}</h2><p><MapPin size={14} /> {event.location} <Clock3 size={14} /> {event.time}</p></div><button className="icon-button">•••</button></article>)}</div></section></> }
