import { CalendarDays, ChevronDown, Search, Users } from 'lucide-react'
import { useState } from 'react'
import { useNavigate } from 'react-router-dom'
import LocationField from '../../../components/LocationField'
import '../../../components/location-fields.css'

const tabs = ['Budget', 'Activity', 'Risk', 'Readiness']
export default function HeroSearch() { const [active, setActive] = useState('Budget'); const [destination, setDestination] = useState(''); const [dates, setDates] = useState('12 Sep - 20 Sep'); const [travellers, setTravellers] = useState(2); const navigate = useNavigate(); function search() { const params = new URLSearchParams({ domain: active, destination, dates, travellers: String(travellers) }); navigate(`/results?${params.toString()}`) } return <section className="hero"><div className="hero-content"><span className="eyebrow eyebrow-light">Travel with clarity</span><h1>Go further.<br /><em>Worry less.</em></h1><p>One calm dashboard for every detail between your front door and somewhere unforgettable.</p><div className="search-panel"><div className="search-tabs">{tabs.map((tab) => <button type="button" key={tab} className={active === tab ? 'selected' : ''} onClick={() => setActive(tab)}>{tab}</button>)}</div><div className="search-fields"><label className="search-location"><LocationField label="Where to?" value={destination} onChange={setDestination} placeholder="Choose a destination" compact /></label><label className="search-date"><CalendarDays size={17} /><span>When<strong><input type="text" value={dates} onChange={(event) => setDates(event.target.value)} /></strong></span></label><label className="traveller-control"><Users size={17} /><span>Travellers<strong>{travellers} traveller{travellers === 1 ? '' : 's'}</strong></span><button type="button" onClick={() => setTravellers((count) => count >= 8 ? 1 : count + 1)}><ChevronDown size={15} /></button></label><button type="button" className="button button-coral" onClick={search}><Search size={17} /> Search</button></div></div></div><div className="hero-note">01 <span>Smart planning for real journeys</span></div></section> }
